<%= turbo_stream.append "messages" do %>
  <%= render "messages/message", messages: @chat.messages.last(2) %>
<% end %>

<%= turbo_stream.update "new_message_container" do %>
  <%= render "messages/form", chat: @chat, message: Message.new %>
<% end %>
